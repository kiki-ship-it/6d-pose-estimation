"""安装说明"""

# 安装指南

1. 创建虚拟环境（推荐）:

```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\\Scripts\\activate
```

2. 安装依赖:

```bash
pip install -r requirements.txt
```

3. （可选）为 YOLOv8 下载权重:

- 将 yolov8m.pt 下载并放到仓库根目录或修改 detector 中的权重路径。
